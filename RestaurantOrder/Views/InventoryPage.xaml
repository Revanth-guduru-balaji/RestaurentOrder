<UserControl x:Class="RestaurantOrder.Views.InventoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d"
             Background="{StaticResource BrandSurfaceBrush}">
    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Inventory" Style="{StaticResource PageTitle}" />
                <TextBlock Text="Manage your menu items, prices, and availability"
                           Style="{StaticResource PageSubtitle}" Margin="0,4,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Download Sample Excel"
                        Style="{StaticResource GhostButton}" Margin="0,0,8,0"
                        Click="DownloadSample_Click" />
                <Button Content="Import from Excel"
                        Style="{StaticResource GhostButton}" Margin="0,0,8,0"
                        Click="ImportExcel_Click" />
                <Button Content="+ Add Item"
                        Style="{StaticResource PrimaryButton}"
                        Click="AddItem_Click" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,18,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchBox"
                         ui:ControlHelper.PlaceholderText="Search items..."
                         FontSize="13"
                         TextChanged="SearchBox_TextChanged" />
                <TextBlock Grid.Column="1" x:Name="CountText" VerticalAlignment="Center"
                           Margin="16,0,0,0" Foreground="{StaticResource BrandSubtleTextBrush}" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="ItemsGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      BorderThickness="0"
                      Background="Transparent"
                      MouseDoubleClick="ItemsGrid_MouseDoubleClick"
                      CellEditEnding="ItemsGrid_CellEditEnding">
                <DataGrid.Resources>
                    <Style x:Key="RightAlignedCell" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                        <Setter Property="HorizontalContentAlignment" Value="Right" />
                    </Style>
                    <Style x:Key="CenterAlignedCell" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                    </Style>
                    <Style x:Key="ActionCell" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="180" />
                    <DataGridTextColumn Header="Price (₹)"
                                        Binding="{Binding Price, StringFormat=N2}"
                                        CellStyle="{StaticResource RightAlignedCell}"
                                        Width="120" />
                    <DataGridCheckBoxColumn Header="Available"
                                            Binding="{Binding IsAvailable}"
                                            CellStyle="{StaticResource CenterAlignedCell}"
                                            Width="100" />
                    <DataGridTemplateColumn Header="" Width="100" CanUserSort="False" IsReadOnly="True"
                                            CellStyle="{StaticResource ActionCell}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete" Click="Delete_Click"
                                        Tag="{Binding Id}"
                                        Style="{StaticResource DangerButton}"
                                        Padding="10,4"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
