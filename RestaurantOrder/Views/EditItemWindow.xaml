<Window x:Class="RestaurantOrder.Views.EditItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        mc:Ignorable="d"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Add / Edit Menu Item"
        Height="400" Width="460"
        SizeToContent="Height"
        MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FocusManager.FocusedElement="{Binding ElementName=NameBox}"
        Background="{StaticResource BrandSurfaceBrush}">
    <StackPanel Margin="22,18">
        <TextBlock Text="Menu item details" Style="{StaticResource SectionTitle}" />

        <TextBlock Text="Name" Margin="0,4,0,4" />
        <TextBox x:Name="NameBox" />

        <TextBlock Text="Category" Margin="0,12,0,4" />
        <TextBox x:Name="CategoryBox" ui:ControlHelper.PlaceholderText="e.g. Breakfast, Lunch, Beverages" />

        <TextBlock Text="Price (₹)" Margin="0,12,0,4" />
        <TextBox x:Name="PriceBox" />

        <CheckBox x:Name="AvailableBox" Content="Available for ordering"
                  IsChecked="True" Margin="0,16,0,0" />

        <Grid Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="Cancel"
                    Style="{StaticResource GhostButton}"
                    IsCancel="True"
                    Click="Cancel_Click" />
            <Button Grid.Column="3" Content="Save"
                    Style="{StaticResource PrimaryButton}"
                    IsDefault="True"
                    Click="Save_Click" />
        </Grid>
    </StackPanel>
</Window>
